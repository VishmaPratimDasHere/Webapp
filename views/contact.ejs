<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Contact — Vishma Pratim Das</title>
    <link rel="stylesheet" href="/css/output-styles.css" />
    <link rel="shortcut icon" href="images/Favicon.png" type="image/x-icon">
</head>

<body class="bg-gray-50 text-gray-900 antialiased min-h-screen flex flex-col">

    <!-- navbar -->
    <%- include("partials/navbar") %>

        <main class="flex-1 pt-24 pb-20">
            <section class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
                <h1 class="text-3xl font-extrabold tracking-tight">Get in touch</h1>
                <p class="mt-3 text-gray-600">
                    I’m best reached by email — please write to me at:
                </p>

                <div class="mt-8 inline-flex flex-col items-center gap-3">
                    <a id="emailLink" href="mailto:vishmapdas@gmail.com"
                        class="text-xl sm:text-2xl font-medium text-blue-600 hover:underline"
                        aria-label="Send email to vishmapdas@gmail.com">
                        vishmapdas@gmail.com
                    </a>

                    <div class="flex gap-3 items-center">
                        <button id="copyBtn"
                            class="px-4 py-2 rounded-md bg-blue-600 text-white text-sm hover:bg-blue-700 transition focus:outline-none focus:ring-2 focus:ring-blue-300">
                            Copy email
                        </button>

                        <a href="mailto:vishmapdas@gmail.com"
                            class="px-4 py-2 rounded-md border border-gray-200 bg-white text-gray-800 text-sm hover:bg-gray-50 transition">
                            Open mail client
                        </a>
                    </div>

                    <p id="copyMsg" class="mt-2 text-sm text-green-600 hidden">Copied to clipboard ✅</p>

                    <p class="mt-6 text-sm text-gray-600 max-w-xl">
                        Please include a short description of your project or the reason for contacting, plus any
                        relevant links.
                        I typically reply within a few business days.
                    </p>
                </div>
            </section>
        </main>

        <!-- footer -->
        <%- include("partials/footer") %>

            <script>
                const copyBtn = document.getElementById('copyBtn');
                const copyMsg = document.getElementById('copyMsg');
                const email = 'vishmapdas@gmail.com';

                copyBtn?.addEventListener('click', async () => {
                    try {
                        await navigator.clipboard.writeText(email);
                        copyMsg.classList.remove('hidden');
                        // hide after 2.2s
                        setTimeout(() => copyMsg.classList.add('hidden'), 2200);
                    } catch (err) {
                        // fallback: select the mailto link text
                        const link = document.getElementById('emailLink');
                        if (link) {
                            const range = document.createRange();
                            range.selectNodeContents(link);
                            const sel = window.getSelection();
                            sel.removeAllRanges();
                            sel.addRange(range);
                        }
                        alert('Copy failed — please select and copy the email manually.');
                    }
                });
            </script>
</body>

</html>